<% flash.each do |name, msg| %>
  <% if name.to_s == "notice" %>
    <%= content_tag :div, msg, :class => "flash-notice vanish" if msg.is_a?(String) %>
  <% else %>
    <%= content_tag :div, msg, :class => "flash-#{name}" if msg.is_a?(String) %>
  <% end %>
<% end %>
